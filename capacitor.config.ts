import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'electronic_classroom_ion',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
